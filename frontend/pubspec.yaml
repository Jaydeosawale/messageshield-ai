name: messageshield
description: Privacy-first message safety assistant
publish_to: none
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter: {sdk: flutter}
  flutter_riverpod: ^2.5.1
  dio: ^5.5.0
  go_router: ^14.2.0
  flutter_secure_storage: ^11.0.0
  http: ^1.6.0
  provider: ^6.1.5+1
flutter:
  uses-material-design: true
